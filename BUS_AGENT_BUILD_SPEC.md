# 빌드 스펙 - 서울 버스 혼잡 추천 AI Agent

> 이 문서는 현재 캡스톤 프로젝트의 AI Agent를 더 정확하게 만들기 위한 구현 요청서다.
> 목표는 "자연어를 그럴듯하게 말하는 챗봇"이 아니라, 현재 가진 버스 혼잡 예측 데이터 안에서만
> 정확하게 추천하고 설명하는 도우미를 만드는 것이다.

---

## 0. 한 줄 목표

사용자가 자연어로 출발지, 도착지, 시간 조건을 말하면, 현재 프로젝트의 예측 데이터 안에서
덜 붐비는 버스 선택지를 추천하고 그 근거를 한국어로 설명하는 AI Agent를 만든다.

## 1. 현재 프로젝트 기준 데이터 범위

Agent는 아래 데이터만 근거로 사용한다.

- `frontend/public/data/selected_routes.json`
- 현재 프론트엔드가 계산한 후보 노선 정보
- 현재 선택 날짜, 시간대, 정류장, 노선 평균 혼잡도

Agent가 하면 안 되는 것:

- 실시간 버스 위치를 지어내기
- 배차 간격, 도착 예정 시간, 환승 시간 추정하기
- 데이터에 없는 노선 추천하기
- 모르는 내용을 사실처럼 말하기

## 2. Agent 역할

Agent의 유일한 역할은 아래 네 가지다.

1. 사용자 문장에서 출발지, 도착지, 시간 조건을 파악한다.
2. 계산된 후보 노선들 중 덜 붐비는 선택지를 설명한다.
3. 현재 선택 시간과 대안 시간을 비교해 준다.
4. 왜 그런 추천이 나왔는지 데이터 기준으로 짧게 설명한다.

Agent의 역할이 아닌 것:

- 일반 대화
- 실시간 교통 상담
- 관광/맛집 추천
- 지도 전체 해설

## 3. 지원해야 하는 질문 유형

### 3-1. 추천형

- "오후 6시에 수유에서 종로 쪽으로 가는데 덜 붐비는 버스 추천해줘"
- "지금 시간 말고 더 한산한 시간도 같이 알려줘"

### 3-2. 비교형

- "오전 8시랑 9시 중 뭐가 덜 붐벼?"
- "지금 선택한 시간보다 나은 시간대가 있어?"

### 3-3. 설명형

- "왜 이 노선을 추천한 거야?"
- "출발 정류장이 많이 붐벼서 그런 거야, 노선 평균이 높아서 그런 거야?"

### 3-4. 제한형

- "6시는 고정하고 노선만 비교해줘"
- "시간은 상관없고 가장 덜 붐비는 때로 추천해줘"

## 4. 응답 규칙

- 항상 한국어로 답한다.
- 2~4문장 안에서 짧게 답한다.
- 추천 결과와 근거를 함께 말한다.
- 숫자를 말할 때는 어떤 값인지 분명히 쓴다.
- 데이터에 없는 것은 모른다고 말한다.
- 과장하거나 확신하는 말투를 쓰지 않는다.

좋은 응답 예시:

> 273번 18시대 후보가 현재 계산된 옵션 중 가장 덜 붐볐습니다.
> 출발 정류장 혼잡도와 노선 평균 혼잡도를 함께 반영했을 때 점수가 가장 낮았습니다.
> 다만 이 결과는 예측 데이터 기준이며, 실제 도착 시각이나 배차 간격은 포함하지 않습니다.

## 5. 시스템 구조

Agent는 두 단계로 나눈다.

### 5-1. 규칙/계산 엔진

프론트엔드 또는 서버에서 먼저 아래를 계산한다.

- 사용자 문장에서 감지한 출발 정류장
- 사용자 문장에서 감지한 도착 정류장
- 사용자 문장에서 감지한 희망 시간
- 시간 고정 여부
- 추천 후보 1~5개
- 각 후보의 근거 수치
  - 출발 정류장 혼잡도
  - 도착 정류장 혼잡도
  - 노선 평균 혼잡도
  - 시간 근접 패널티

### 5-2. LLM

LLM은 계산을 새로 하지 않고, 위에서 만든 구조화된 JSON을 받아서:

- 추천 결과를 사람에게 읽기 쉽게 요약하고
- 주의사항을 덧붙이고
- 데이터 한계를 분명히 밝히는 역할만 한다.

즉, "계산은 코드", "설명은 LLM" 구조로 간다.

## 6. 성능 개선 원칙

현재 프로젝트의 Agent 성능을 높이려면 아래 원칙을 따른다.

1. 질문 범위를 좁힌다.
2. LLM에 원본 전체를 무작정 던지지 않는다.
3. 코드가 먼저 계산한 결과를 LLM에 넘긴다.
4. 모호한 입력은 재질문하게 만든다.
5. 추천 근거 수치를 항상 응답에 연결한다.

## 7. 단계별 구현 계획

1. 현재 `/api/llm-analysis`를 Gemini 기반 서버 호출로 바꾼다.
2. 환경변수를 `GEMINI_API_KEY`, `GEMINI_MODEL` 기준으로 정리한다.
3. 사용자 질문 파싱 로직을 테스트 가능한 함수로 분리한다.
4. 추천 후보 계산 결과를 표준 JSON 형태로 고정한다.
5. LLM 프롬프트를 "설명 전용" 역할에 맞게 좁힌다.
6. 고정 평가 질문 세트를 만들어 매번 검증한다.

## 8. 평가 질문 세트

아래 질문들은 수정할 때마다 반복 확인한다.

1. "오후 6시에 수유에서 종로 쪽으로 가는데 덜 붐비는 버스 추천해줘"
2. "6시는 고정하고 노선만 비교해줘"
3. "지금보다 한산한 시간대도 같이 알려줘"
4. "왜 그 노선을 추천한 거야?"
5. "출발 정류장이 붐벼서 그런 거야?"
6. "오전 8시랑 9시 중 뭐가 덜 붐벼?"
7. "시간 상관없이 가장 한산한 때로 추천해줘"
8. "강남역 가는 지하철 추천해줘"

8번 같은 질문에는 버스 혼잡 추천 범위를 벗어난다고 분명히 답해야 한다.

## 9. 완료 기준

아래가 모두 만족되면 1차 완료다.

- Gemini 키를 서버 전용 환경변수로 읽는다.
- 브라우저 코드에 LLM 키가 노출되지 않는다.
- 추천형, 비교형, 설명형 질문에 일관된 형식으로 답한다.
- 데이터 밖의 질문에는 추측하지 않는다.
- 평가 질문 세트에서 오답/환각이 눈에 띄게 줄어든다.
